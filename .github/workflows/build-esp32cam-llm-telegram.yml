name: Build esp32cam-llm-telegram

on:
  push:
    branches: [main, develop, 'claude/**']
    paths:
      - 'packages/camera-vision/llm-telegram/**'
      - '.github/workflows/build-esp32cam-llm-telegram.yml'
      - '.github/workflows/_ci-build-esp32.yml'
  pull_request:
    branches: [main, develop]
    paths:
      - 'packages/camera-vision/llm-telegram/**'
      - '.github/workflows/build-esp32cam-llm-telegram.yml'
      - '.github/workflows/_ci-build-esp32.yml'
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      release_tag:
        description: 'Release tag to build for (leave empty for CI build)'
        required: false
        type: string

jobs:
  ci-build:
    if: github.event_name == 'push' || github.event_name == 'pull_request' || (github.event_name == 'workflow_dispatch' && inputs.release_tag == '')
    uses: ./.github/workflows/_ci-build-esp32.yml
    with:
      project: esp32cam-llm-telegram
      project_path: camera-vision/llm-telegram
      target: esp32

  check-tag:
    if: github.event_name == 'release' || (github.event_name == 'workflow_dispatch' && inputs.release_tag != '')
    runs-on: ubuntu-latest
    outputs:
      should_build: ${{ steps.check.outputs.should_build }}
      tag: ${{ steps.check.outputs.tag }}
    steps:
      - id: check
        run: |
          TAG="${{ github.event.release.tag_name || inputs.release_tag }}"
          echo "tag=$TAG" >> $GITHUB_OUTPUT
          if [[ "$TAG" == "esp32cam-llm-telegram@v"* ]] || [[ "${{ github.event_name }}" == "workflow_dispatch" ]]; then
            echo "should_build=true" >> $GITHUB_OUTPUT
          else
            echo "should_build=false" >> $GITHUB_OUTPUT
          fi

  release-build:
    needs: check-tag
    if: needs.check-tag.outputs.should_build == 'true'
    uses: ./.github/workflows/_build-esp32-firmware.yml
    with:
      project: esp32cam-llm-telegram
      project_path: camera-vision/llm-telegram
      target: esp32
      binary_name: esp32cam-llm-telegram.bin
      release_tag: ${{ needs.check-tag.outputs.tag }}
    secrets: inherit
