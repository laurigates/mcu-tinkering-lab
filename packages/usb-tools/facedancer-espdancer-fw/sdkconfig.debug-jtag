# Debug overlay — keeps USB-Serial-JTAG alive for log output.
# Disables the raw DCD path so the PHY stays with JTAG (no USB emulation).
# Use only for comms/control-channel bring-up without USB.
# Usage: rm sdkconfig && just build-debug-jtag

CONFIG_USJ_ENABLE_USB_SERIAL_JTAG=y
CONFIG_ESP_CONSOLE_SECONDARY_USB_SERIAL_JTAG=y
CONFIG_ESP_CONSOLE_SECONDARY_NONE=n