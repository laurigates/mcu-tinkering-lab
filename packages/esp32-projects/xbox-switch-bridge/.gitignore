# ESP-IDF build artifacts
build/
sdkconfig
sdkconfig.old

# Managed components (downloaded by IDF component manager)
managed_components/
dependencies.lock

# External dependencies (cloned at build time)
external/

# IDE files
.vscode/
.idea/
*.swp
*.swo
*~

# Credentials (belt-and-suspenders — also covered by repo root .gitignore)
credentials.h

# macOS/Windows
.DS_Store
Thumbs.db
