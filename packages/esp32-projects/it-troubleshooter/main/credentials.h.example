/**
 * @file credentials.h
 * @brief WiFi and API credentials (do not commit actual values).
 *
 * Copy this file to credentials.h and fill in your values.
 * credentials.h is gitignored.
 */
#pragma once

/* WiFi (mobile hotspot) */
#define WIFI_SSID "your-hotspot-ssid"
#define WIFI_PASS "your-hotspot-password"

/* Claude API */
#define CLAUDE_API_KEY "sk-ant-..."

/* Optional: SSH access to managed hosts */
/* #define SSH_USER "admin" */
/* #define SSH_PASS "password" */
