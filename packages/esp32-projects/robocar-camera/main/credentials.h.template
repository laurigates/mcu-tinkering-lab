/**
 * @file credentials.h.template
 * @brief Template for WiFi and API credentials - SECURITY CRITICAL
 *
 * SECURITY INSTRUCTIONS:
 * 1. Copy this file to credentials.h
 * 2. Fill in your actual credentials
 * 3. NEVER commit credentials.h to the repository
 * 4. Use environment variables for CI/CD deployments
 *
 * IMPORTANT: credentials.h is in .gitignore to prevent accidental commits
 */

#ifndef CREDENTIALS_H
#define CREDENTIALS_H

// WiFi Configuration
// Replace with your actual WiFi network credentials
#define WIFI_SSID "YOUR_WIFI_SSID_HERE"
#define WIFI_PASSWORD "YOUR_WIFI_PASSWORD_HERE"

// Claude API Configuration
// Get your API key from: https://console.anthropic.com/
// CRITICAL: Rotate any exposed keys immediately
#define CLAUDE_API_KEY "sk-ant-api03-YOUR_ACTUAL_API_KEY_HERE"

// Security Notes:
// - Never share these credentials in public repositories
// - Rotate API keys regularly (recommended: every 90 days)
// - Use different credentials for development vs production
// - For production deployment, use environment variables:
//   export CLAUDE_API_KEY="your-key"
//   export WIFI_SSID="your-ssid"
//   export WIFI_PASSWORD="your-password"

#endif // CREDENTIALS_H
